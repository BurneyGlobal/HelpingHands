
<!-- <div class="container blk-background">
  <%#= render 'shared/list_card' %>
</div> -->

<div class="wrap blk-background">

    <div class="grid">


      <div
        id="map"
        style="width: 900px;
        height: 500px;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
      ></div>

      <div class="count"> <%= @volunteers.count %> </div>
      <div class="card"> <%= render 'shared/list_card' %> </div>
    </div>
  </div>
</div>

